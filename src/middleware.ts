import { NextResponse } from 'next/server'

export async function middleware() {
  // Auth bypassed for personal development
  return NextResponse.next()
}

export const config = {
  matcher: [
    '/((?!_next/static|_next/image|favicon.ico|.*\\.(?:svg|png|jpg|jpeg|gif|webp)$).*)',
  ],
}